import { LoginForm } from "../components/LoginForm";

export function Login () {
    return (
        <section className=" flex w-full h-dvh items-center justify-center bg-linear-to-br from-orange-50 to-red-100">
            <LoginForm></LoginForm>
        </section>
    )
}