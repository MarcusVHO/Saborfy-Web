export interface OrderFilterData {
    name: string | null,
    status:string | null,
    orderNumber:number | null,
    street:string | null,
    addressNumber:number | null,
    startDate: Date | null,
    endDate:Date | null
}